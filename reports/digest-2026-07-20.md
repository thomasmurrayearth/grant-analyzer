# Grant Analyzer — Weekly Digest
**Week of 2026-07-13 to 2026-07-20**

## Headline numbers

| Metric | This week | Previous week (07-06 to 07-13) |
|---|---|---|
| Analyses started | 2 | 1 |
| Analyses completed | 1 | 1 |
| Analyses failed (error_message set) | 0 | 0 |
| Analyses stuck/abandoned (started but not completed, no error) | 1 | 0 |

For context: the app has logged **15 analyses in total** since it started tracking, so this is still a very low-traffic, early-stage tool — 2 runs in a week is normal at this stage, not a red flag on its own.

## What people searched for

| Company | Website | Geographies specified | Consortium / Accelerators / Prizes | Result |
|---|---|---|---|---|
| Notpla | notpla.com | *(not specified)* | Yes / Yes / Yes | Got as far as the company profile (Phase 1), then **stopped** — never moved into the grant search phase |
| Fermeta | linkedin.com/company/fermeta | *(not specified)* | Yes / Yes / Yes | Completed in ~13 minutes, found **3 grants**, cost $0.57 |

Both users left every filter (consortium, accelerator, prize eligibility) switched on and left the "geographies" field blank — worth keeping an eye on whether that's a real signal (users don't know what to put there) or just this small a sample being coincidence.

## Completed analyses

- **1 of 2** analyses this week reached "completed" status.
- Average grants found (completed analyses only): **3**.
- Cost for the completed run: **$0.57**.

## Errors

No error messages were logged this week — the `error_message` column was empty for both rows, and there were no failed-status runs. Nothing to group.

## Anything unusual

- **Notpla's analysis stalled at "profile_ready"** — that's the state right after Phase 1 (the company profile) finishes, before Phase 2/3 (the actual grant search) starts. It has no `completed_at` and no error, so this isn't a crash the app caught — it's either:
  1. The user saw the company profile and simply didn't click "continue" (a normal, healthy drop-off), or
  2. The `/analyse/continue` call failed in a way that isn't being logged to `error_message` (a silent failure worth checking).

  Recommend checking browser/network logs or asking Railway logs from around 2026-07-13 10:23–11:00 UTC to tell which of these it was — I didn't do this as part of this read-only digest since it goes beyond a database query, but it's a quick follow-up if you want it done.

## Ideas to improve the app

1. **Instrument the profile→continue drop-off.** Right now a stalled run (like Notpla's) looks identical in the database whether the user walked away or the backend silently failed. Adding a lightweight event log entry when `/analyse/continue` is called (and another if it errors) would let next week's digest tell those two cases apart automatically instead of guessing.
2. **Check whether "geographies" is actually reachable in the frontend.** It's been blank in both of this week's runs (and it's an optional field per the schema) — if that's because the field is easy to miss or unclear what to type, tightening the label/placeholder could improve grant-matching relevance, since geography is a real scoring input.
3. **At this volume (1-2 users/week), a personal follow-up beats aggregate analytics.** With only 2 runs, error-pattern analysis won't be statistically meaningful for a while. A quick manual check-in with these two users (if you have contact info) about what they were hoping to find would likely teach you more than another week of automated digests.

---
*Generated automatically from the `analyses` table in Supabase. Read-only — no data or code was changed.*
